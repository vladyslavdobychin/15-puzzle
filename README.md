### Using Docker Compose

Build and run in one command:
```bash
docker compose run --rm puzzle
```

### Using Native PHP

Install dependencies:
```bash
composer install
```

Run the game:
```bash
composer play
```

Or directly:
```bash
php play.php
```